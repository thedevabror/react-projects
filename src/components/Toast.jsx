import React from "react";
import { ToastContainer, toast } from "react-toastify";

const Toast = () => {
  const notify = () => toast.success("Maxsulot qo'shildi");
  return (
<></>
  );
};

export default Toast;
